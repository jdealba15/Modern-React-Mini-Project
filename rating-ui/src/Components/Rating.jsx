const Rating = () => {
    return (
        <div style={styles.container}>
            <h2>Rate Your Expereince</h2>
        </div>
    );
};

const styles = {
    container: {
        textAlign: 'center',
        fontFamily: 'Arial',
        padding: '20px',
    },
};

export default Rating;